# Whether you want the Python script to prompt for fields if already specified here
INPUT = False

# Github user and repo
GH_USER = u'gh_user'
GH_REPO = u'gh_repo'

# Bitbucket user and repo
BB_USER = u'bb_user'
BB_REPO = u'bb_repo'

# Github authentication token
GH_TOKEN = 'gh_token'

# map bitbucket usernames to github usernames
# bitbucket_user -> github_user
USER_MAP = {
	u'mittsh': u'mittsh',
}
